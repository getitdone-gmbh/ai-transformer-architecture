[project]
name = "ai-transformer-architecture"
version = "0.1.0"
description = "GPT-style transformer trained from scratch in PyTorch — 540M-param German model, pretraining + SFT + inference"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.2.0",
    "numpy>=2.3.4",
    "plotly>=6.7.0",
    "tiktoken>=0.12.0",
    "torch>=2.12.0",
    "transformers>=5.14.1",
]
